Tabs — the glowing indicator springs between items; optional mono count per tab. Below 900px the strip scrolls horizontally while the marker tracks the active tab; coarse pointers use 44px tab targets.

```jsx
<Tabs items={[{value:'all',label:'All',count:128},'Failed']} value={tab} onChange={setTab}/>
```
