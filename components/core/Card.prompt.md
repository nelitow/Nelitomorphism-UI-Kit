Card — the base raised surface. `material` turns it into a hero card with live WebGL iridescence + scanlines; `glow` adds an accent ring on hover.

```jsx
<Card title="Revenue" meta="synced 4s ago">…</Card>
<Card material glow>hero content</Card>
```
