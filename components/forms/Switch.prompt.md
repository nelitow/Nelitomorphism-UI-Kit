Switch — pill toggle; thumb springs across and the track glows cyan when on. On touch the full label is a 44px target while the track remains visually compact.

```jsx
<Switch label="Auto-deploy" checked={on} onChange={setOn}/>
```
