Checkbox — the check stroke draws in with a spring; checked box glows cyan. On touch the full label is a 44px target while the box stays compact.

```jsx
<Checkbox label="Enable tracing" checked={on} onChange={setOn}/>
```
