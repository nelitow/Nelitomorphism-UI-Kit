FileUpload — dashed drop zone that lights up and glows on drag-over; dropped files appear as rows with live progress bars.

```jsx
<FileUpload label="Drop CSV exports" accept=".csv" onFiles={handle}/>
```
