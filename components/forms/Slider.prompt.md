Slider — pointer-driven track with glowing fill; thumb blooms while dragging; mono readout lights up.

```jsx
<Slider label="Timeout" min={0} max={5000} step={100} value={v} onChange={setV} unit="ms"/>
```
