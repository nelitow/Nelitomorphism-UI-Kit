Select — custom dropdown with glass popover; options cascade in with 25ms stagger. Touch uses 44px controls and a viewport-clamped, internally scrolling popup.

```jsx
<Select label="Region" options={["us-east","eu-west","ap-south"]} value={r} onChange={setR}/>
```
