Input — compact field; focus springs a glowing underline from the center and lights the label. Touch fields use a 44px control with 16px text to avoid iOS focus zoom.

```jsx
<Input label="API key" mono placeholder="sk-…" hint="Scoped to this project"/>
<Input label="Email" error="Address already in use"/>
```
