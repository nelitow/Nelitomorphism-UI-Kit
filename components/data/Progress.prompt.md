Progress — 4px track with glowing head; width springs to new values. Omit `value` for the indeterminate light sweep.

```jsx
<Progress label="Index rebuild" value={62}/>
<Progress label="Uploading" tone="accent2"/>
```
