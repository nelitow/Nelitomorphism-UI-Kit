Progress — 4px track with glowing head; width springs to new values. Omit `value` for the indeterminate light sweep. Long labels truncate instead of pushing the value off narrow layouts.

```jsx
<Progress label="Index rebuild" value={62}/>
<Progress label="Uploading" tone="accent2"/>
```
