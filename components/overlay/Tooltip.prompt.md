Tooltip — mono glass tip that springs toward the target after 250ms hover.

It is hover-only: coarse pointers render the child without a tooltip bubble.

```jsx
<Tooltip content="p95 · last 24h"><Badge tone="warn">142ms</Badge></Tooltip>
```
